```mermaid
classDiagram
    class User {
        +String userId
        +String username
        +String password
        +String email
        +Enum role
        +login() Boolean
        +logout() void
        +viewDashboard() void
    }
    class Owner {
        +manageOrganizations() void
        +manageSystemSettings() void
    }
    class OrganisationOwner {
        +manageDevices() void
        +manageUsers() void
        +manageLocations() void
    }
    class AdminManager {
        +monitorAlerts() void
        +reviewDeviceStatus() void
    }
    class Inspector {
        +viewReadings() void
        +followUpIncidents() void
    }
    class EmbeddedDevice {
        +String deviceId
        +String location
        +String status
        +Boolean isActive
        +connectToNetwork() void
        +sendData(payload: String) void
        +calibrate() void
    }
    class TemperatureSensor {
        +String sensorId
        +Float currentTemp
        +Float calibrationOffset
        +readTemperature() Float
        +isCalibrated() Boolean
    }
    class MQTTBroker {
        +String brokerUrl
        +Int port
        +String topic
        +publish(topic, message) void
        +subscribe(topic) void
    }
    class Server {
        +String serverId
        +String status
        +receiveData(payload) void
        +processData(temp) void
        +checkThreshold(temp) Boolean
        +triggerAlert() void
    }
    class Database {
        +String dbConnection
        +saveTemperatureLog() void
        +saveAlertLog() void
        +fetchDeviceRecords() List
        +fetchAlertHistory() List
    }
    class Alert {
        +String alertId
        +String deviceId
        +Float temperature
        +DateTime timestamp
        +Enum status
        +trigger() void
        +resolve() void
        +notifyUser() void
    }
    class Dashboard {
        +String dashboardId
        +String currentUserId
        +displayLiveReadings() void
        +showAlert(alert) void
        +updateDeviceStatus() void
    }
    class ThresholdConfig {
        +String configId
        +Float thresholdValue
        +Boolean isActive
        +updateThreshold(value) void
        +getThreshold() Float
    }


    User <|-- Owner
    User <|-- OrganisationOwner
    User <|-- AdminManager
    User <|-- Inspector
    EmbeddedDevice *-- TemperatureSensor
    Server *-- ThresholdConfig
    EmbeddedDevice --> MQTTBroker
    MQTTBroker --> Server
    Server --> Database
    Dashboard --> Server
    User --> Dashboard
    Server --> Alert

    
    Alert --> Owner : sends notification
    Alert --> AdminManager : sends notification
    Alert --> Inspector : sends notification
